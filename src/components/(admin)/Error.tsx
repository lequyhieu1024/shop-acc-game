export default function ErrorPage() {
    return (
        <div className={`d-flex justify-content-center`}>
            <h2 className={`text-danger`}>Có lỗi sảy ra, vui lòng thử lại</h2>
        </div>
    )
}